% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neurona.R
\name{rnas_neuron_forward_batch}
\alias{rnas_neuron_forward_batch}
\title{Evaluar una neurona RNAS para un conjunto de observaciones}
\usage{
rnas_neuron_forward_batch(X, w, b, activation = "tanh", devolver_z = FALSE)
}
\arguments{
\item{X}{Matriz numerica de entradas.}

\item{w}{Vector numerico de pesos.}

\item{b}{Sesgo numerico escalar.}

\item{activation}{Funcion de activacion o nombre de activacion.
Puede ser una funcion R o una cadena: `"sigmoid"` o `"tanh"`.}

\item{devolver_z}{Valor logico. Si es `TRUE`, devuelve tambien los potenciales internos.}
}
\value{
Si `devolver_z = FALSE`, retorna un vector de salidas estimadas.
Si `devolver_z = TRUE`, retorna una lista con `z`, `y_hat` y `activation`.
}
\description{
Evalua una neurona individual sobre una matriz de entradas `X`.
Cada fila de `X` representa una observacion y cada columna una variable.
}
\examples{
X <- matrix(c(0.5, -0.2,
              1.0,  0.3,
             -0.7,  0.8), ncol = 2, byrow = TRUE)
w <- c(0.8, 0.3)
b <- 0.1

rnas_neuron_forward_batch(X, w, b, activation = "tanh")

}
