% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control_eta.R
\name{rnas_train_neuron_control_eta}
\alias{rnas_train_neuron_control_eta}
\title{Entrenar neurona RNAS con control de tasa de aprendizaje}
\usage{
rnas_train_neuron_control_eta(
  X,
  y,
  theta0,
  eta0 = 0.1,
  policy = "constante",
  eta_min = 1e-05,
  eta_max = 1,
  T = 100,
  activation = "tanh",
  ventana_regimen = 5L,
  tau_loss = 0.01,
  tau_grad = 0.001,
  eps_loss = 1e-08,
  eps_grad = 1e-04
)
}
\arguments{
\item{X}{Matriz numerica de entradas.}

\item{y}{Vector numerico de valores observados.}

\item{theta0}{Vector inicial de estado con pesos y sesgo.}

\item{eta0}{Tasa inicial positiva.}

\item{policy}{Politica de tasa. Texto o lista con campo `tipo`.}

\item{eta_min}{Cota inferior de tasa.}

\item{eta_max}{Cota superior de tasa.}

\item{T}{Numero entero positivo de iteraciones.}

\item{activation}{Nombre de activacion. Puede ser `"tanh"` o `"sigmoid"`.}

\item{ventana_regimen}{Ventana usada para clasificar regimenes parciales.}

\item{tau_loss}{Umbral de reduccion relativa.}

\item{tau_grad}{Umbral de gradiente activo.}

\item{eps_loss}{Tolerancia de perdida.}

\item{eps_grad}{Tolerancia de gradiente.}
}
\value{
Objeto de clase `rnas_control_eta_train`.
}
\description{
Ejecuta entrenamiento de una neurona individual usando una tasa de
aprendizaje controlada por una politica. La regla de actualizacion es:
\eqn{\theta_{k+1} = \theta_k - \eta_k \nabla L(\theta_k)}.
}
\details{
La politica de control puede definirse como texto o como lista.
Cuando se usa una lista, pueden especificarse factores multiplicativos
gamma para regular la tasa de aprendizaje. Estos factores modifican
eta segun la regla general eta[k + 1] = gamma[k] * eta[k].

Para politica por regimen, se admiten campos como:
`gamma_refinamiento`, `gamma_saturacion`,
`gamma_inestabilidad` y `gamma_estabilizacion`.
Para politica por mejora relativa, se admite `gamma`.
}
\examples{
X <- matrix(c(0.5, -0.2,
              1.0,  0.3,
             -0.7,  0.8,
              0.0,  0.0), ncol = 2, byrow = TRUE)
y <- c(0.4, 0.8, -0.2, 0.1)
theta0 <- c(0.8, 0.3, 0.1)
res <- rnas_train_neuron_control_eta(X, y, theta0, eta0 = 0.1, T = 20)
res$metricas

}
